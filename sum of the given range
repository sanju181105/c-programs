#include <iostream>
using namespace std;

int main() {
    int num;
    int total_sum = 0;

    do {
        cin >> num;
        if (num != -1) {
            total_sum += num;
        }
    } while (num != -1);

    cout << total_sum << endl;

    return 0;
}
